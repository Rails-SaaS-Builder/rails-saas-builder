<%# Override this partial in your host app at app/views/rsb/auth/account/_identity_fields.html.erb
    to add identity-specific fields (metadata, profile, etc.).
    Available locals: f (form builder), identity (current identity). %>
